export default function Home() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Welcome to NoteHub (Auth)</h1>
      <p>Next.js App Router + React Query + Axios + Cookie auth.</p>
    </main>
  );
}
